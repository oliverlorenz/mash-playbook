# SPDX-FileCopyrightText: 2026 Oliver Lorenz
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
# Deliberately does not unload the batman-adv kernel module or remove
# /etc/modules-load.d/batman-adv.conf: other things on the host may rely on it,
# and unloading a kernel module implicitly on every disable run is too blunt.
# Remove it yourself if you're sure nothing else needs it.

- name: Check existence of ffc-mesh-client systemd service
  ansible.builtin.stat:
    path: "{{ devture_systemd_docker_base_systemd_path }}/{{ ffc_mesh_client_identifier }}.service"
  register: ffc_mesh_client_service_stat

- name: Uninstall ffc-mesh-client systemd services and files
  when: ffc_mesh_client_service_stat.stat.exists | bool
  block:
    - name: Ensure ffc-mesh-client systemd service is stopped
      ansible.builtin.service:
        name: "{{ ffc_mesh_client_identifier }}"
        state: stopped
        enabled: false
        daemon_reload: true

    - name: Ensure ffc-mesh-client systemd service does not exist
      ansible.builtin.file:
        path: "{{ devture_systemd_docker_base_systemd_path }}/{{ ffc_mesh_client_identifier }}.service"
        state: absent

    - name: Ensure ffc-mesh-client container network does not exist via community.docker.docker_network
      when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
      community.docker.docker_network:
        name: "{{ ffc_mesh_client_container_network }}"
        state: absent

    - name: Ensure ffc-mesh-client container network does not exist via ansible.builtin.command
      when: devture_systemd_docker_base_container_network_creation_method == 'command'
      ansible.builtin.command:
        cmd: >-
          {{ devture_systemd_docker_base_host_command_docker }} network rm
          {{ ffc_mesh_client_container_network }}
      register: ffc_mesh_client_network_deletion_result
      changed_when: ffc_mesh_client_container_network in ffc_mesh_client_network_deletion_result.stdout

    - name: Ensure ffc-mesh-client path does not exist
      ansible.builtin.file:
        path: "{{ ffc_mesh_client_base_path }}"
        state: absent
